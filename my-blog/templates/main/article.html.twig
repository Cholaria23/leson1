{% extends 'base.html.twig' %} {% block body %} {% include ("main/header.html.twig") %}
<style>
.example-wrapper {
    margin: 1em auto;
    max-width: 800px;
    width: 95%;
    font: 18px/1.5 sans-serif;
}

.example-wrapper code {
    background: #F5F5F5;
    padding: 2px 6px;
}

label {
    display: block;
    margin-bottom: 0.5rem;
}
</style>
<div class="example-wrapper">
    <h2>{{ post.title }}!</h2>
    <div class="card-footer text-muted">
        Posted on {{ post.datapost|date("m.d.Y") }} by
        <a href="#">{{ post.authtor }}</a>
    </div>
    <img class="card-img-top" src="{{ asset(post.image) }}" alt="Card image cap">
    <p>{{ post.content|raw}} </p>
    <hr></hr>
    
   {% include ('main/partial/comment.html.twig')%}
    {# {{render (controller ('App\\Controller\\MainController::comment') )}} #}
    
    <!-- #respond -->
    <hr></hr>
    <!-- Pagination -->
    <ul class="pagination justify-content-center mb-4">
        <li class="page-item">
            <a class="page-link" href="#{# {{ path('show',{'postSlug':post.slug,'categorySlug':category.slug}) }} #}">&larr; Older</a>
        </li>
        <li class="page-item disabled">
            <a class="page-link" href="#">Newer &rarr;</a>
        </li>
    </ul>
</div>
{% include '/main/footer.html.twig' %} {% endblock %}